<Window x:Class="MfcQueueSystem.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оператор" Height="500" Width="900" 
        Background="{StaticResource BackgroundColor}" WindowStartupLocation="CenterScreen">

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: Инфо -->
        <StackPanel>
            <TextBlock Text="Сотрудник" Foreground="Gray" FontSize="14"/>
            <!-- ИСПРАВЛЕНО: Убрали Style="..." -->
            <ComboBox x:Name="EmployeesCombo" Height="40" Margin="0,5,0,20" FontSize="16"
                      DisplayMemberPath="FullName" SelectedValuePath="EmployeeId" SelectionChanged="EmployeesCombo_SelectionChanged"/>

            <Border Background="White" CornerRadius="10" Padding="20" Margin="0,10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Статус окна" Foreground="Gray"/>
                    <TextBlock x:Name="CurrentWindowText" Text="Не выбрано" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Правая панель: Управление -->
        <Border Grid.Column="1" Background="White" CornerRadius="20" Margin="30,0,0,0" Padding="40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.1" ShadowDepth="5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Текущий вызов" FontSize="18" Foreground="Gray" HorizontalAlignment="Center"/>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="CurrentTicketText" Text="---" FontSize="80" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="ServiceText" Text="Ожидание начала работы..." FontSize="18" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,10"/>
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BtnCall" Content="📢 ВЫЗВАТЬ" Click="BtnCall_Click" Style="{StaticResource ModernBtn}" IsEnabled="False" Height="50" Background="#4CAF50"/>
                    <Button x:Name="BtnFinish" Grid.Column="2" Content="🏁 ЗАВЕРШИТЬ" Click="BtnFinish_Click" Style="{StaticResource ModernBtn}" IsEnabled="False" Height="50" Background="#F44336"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>