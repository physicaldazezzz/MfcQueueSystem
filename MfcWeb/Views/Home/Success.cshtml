@model MfcWeb.Models.Ticket
@{
    ViewData["Title"] = "Успешно";
}

<div class="container text-center mt-5 fade-in">
    <div class="card shadow-lg border-0 rounded-4 mx-auto" style="max-width: 500px; overflow: hidden;">
        <div class="bg-success text-white p-4">
            <h1 class="display-1">✅</h1>
            <h2 class="fw-bold">Вы записаны!</h2>
        </div>
        <div class="card-body p-5">
            <p class="text-muted mb-1">Дата и время визита</p>
            <h3 class="fw-bold mb-4">@Model.AppointmentTime?.ToString("dd MMMM, HH:mm")</h3>

            <hr class="my-4">

            <p class="text-muted mb-2">Ваш код бронирования</p>
            <div class="bg-light rounded-3 p-3 mb-3">
                <h1 class="display-3 fw-bold text-primary" style="letter-spacing: 10px;">@Model.BookingCode</h1>
            </div>

            <div class="alert alert-warning small">
                <i class="bi bi-info-circle"></i> Введите этот код в терминале МФЦ ("У меня есть запись"), чтобы получить талон без очереди.
            </div>

            <a asp-action="Index" class="btn btn-outline-secondary w-100 mt-3 rounded-pill py-3 shadow-lg">
                Вернуться на главную
            </a>
        </div>
    </div>
</div>
